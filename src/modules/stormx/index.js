// Módulo StormX — PLACEHOLDER.
// Sin integración actualmente. Ver ./README.md para el contrato esperado.

function createStormxModule(_deps) {
    return {
        name: 'stormx',
        subscribe: [],
        start() { /* no-op hasta definir contrato */ },
        stop()  { /* no-op */ },
    };
}

module.exports = { createStormxModule };
