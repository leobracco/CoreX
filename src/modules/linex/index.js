// Módulo LineX — PLACEHOLDER.
// Sin integración actualmente. Ver ./README.md para el contrato esperado.

function createLinexModule(_deps) {
    return {
        name: 'linex',
        subscribe: [],
        start() { /* no-op hasta definir contrato */ },
        stop()  { /* no-op */ },
    };
}

module.exports = { createLinexModule };
